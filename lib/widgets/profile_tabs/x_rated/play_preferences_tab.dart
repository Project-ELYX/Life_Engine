// lib/widgets/profile_tabs/x_rated/play_preferences_tab.dart
import 'package:flutter/material.dart';

class PlayPreferencesTab extends StatelessWidget {
  const PlayPreferencesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Play Preferences Tab - TODO: Add UI for soft limits, preferences, and dynamic tags'),
    );
  }
}
