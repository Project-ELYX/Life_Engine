// lib/widgets/profile_tabs/x_rated/hard_limits_tab.dart
import 'package:flutter/material.dart';

class HardLimitsTab extends StatelessWidget {
  const HardLimitsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Hard Limits Tab - TODO: Add checklist or tag-style UI for non-negotiable limits'),
    );
  }
}
