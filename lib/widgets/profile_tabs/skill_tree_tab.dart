import 'package:flutter/material.dart';

class SkillTreeTab extends StatelessWidget {
  const SkillTreeTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Skill Tree'));
  }
}
