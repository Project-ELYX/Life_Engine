// lib/screens/couple_achievements_screen.dart
import 'package:flutter/material.dart';

class CoupleAchievementsScreen extends StatelessWidget {
  const CoupleAchievementsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Load shared milestones between user + partner
    return Scaffold(
      appBar: AppBar(title: const Text("Couple Achievements")),
      body: const Center(
        child: Text("Coming soon: spooky shared chaos achievements 👀"),
      ),
    );
  }
}
